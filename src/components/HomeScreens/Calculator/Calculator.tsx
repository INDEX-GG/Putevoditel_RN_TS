import React from "react";
import { View, Text } from "react-native";

const Calculator = () => {
  return (
    <View>
      <Text>123</Text>
    </View>
  );
};

export default React.memo(Calculator);
