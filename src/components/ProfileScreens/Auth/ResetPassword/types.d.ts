import { NativeStackScreenProps } from "@react-navigation/native-stack";
import { ProfileStackParams } from "../../../../screens/types";

export type TypesResetPasswordProps = NativeStackScreenProps<
  ProfileStackParams,
  "ResetPassword"
>;
